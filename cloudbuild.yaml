steps:
    - name: 'Run NPM install prod'
      entrypoint: 'npm'
      id: 'npm'
      args: ['install', '--production', '--legacy-peer-deps']
      dir: 'frontend'
    - name: 'Run Yarn Build'
      entrypoint: 'yarn'
      args: ['build', '--production']
      dir: 'frontend'
      waitFor: ['npm']
timeout: '3600s'

options:
  logging: CLOUD_LOGGING_ONLY