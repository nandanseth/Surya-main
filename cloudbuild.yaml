steps:
    - name: 'node:16'
      entrypoint: 'npm'
      id: 'npm'
      args: ['install', '--production']
      dir: 'frontend'
    - name: 'node:16'
      entrypoint: 'yarn'
      args: ['build', '--production']
      dir: 'frontend'
      waitFor: ['npm']
timeout: '3600s'

options:
  logging: CLOUD_LOGGING_ONLY