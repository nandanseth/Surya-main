steps:
    - name: 'node'
      entrypoint: 'yarn'
      args: ['build']
      dir: 'frontend'
    - name: 'gcr.io/cloud-builders/gcloud'
      args: ['app', 'deploy', 'frontend/app.yaml']
      dir: 'frontend'
timeout: '600s'
options:
  logging: CLOUD_LOGGING_ONLY