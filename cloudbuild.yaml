steps:
    - name: 'node'
      entrypoint: 'yarn'
      args: ['build', '--verbose']
      dir: 'frontend'
    - name: 'gcr.io/cloud-builders/gcloud'
      args: ['app', 'deploy', 'app.yaml']
      dir: 'frontend'
timeout: '600s'
options:
  logging: CLOUD_LOGGING_ONLY